# BST-tree
# Binary Search Tree (Двоично дърво за търсене)

## Описание
Този проект представлява конзолно приложение на C#, което реализира **двоично дърво за търсене (Binary Search Tree)**.  
Програмата позволява добавяне на елементи, търсене на елемент и обхождане на дървото чрез In-order обход.

Проектът е създаден с учебна цел.

---

## Функционалности
- ➕ Добавяне на елемент в дървото
- 🔍 Търсене на елемент
- 🌲 In-order обхождане на дървото
- ❌ Изход от програмата

---

## Структура на проекта
Проектът съдържа следните класове:

- **Program.cs**
  - Съдържа `Main()` метода
  - Меню за избор на действия от потребителя

- **BinarySearchTree.cs**
  - Реализация на двоично дърво за търсене
  - Методи:
    - `Insert(int value)`
    - `Search(int value)`
    - `InOrderTraversal()`

- **Node.cs**
  - Представлява възел в дървото
  - Съдържа стойност и препратки към ляво и дясно дете

---

## Как работи програмата
1. При стартиране се показва меню
2. Потребителят избира действие чрез цифра
3. В зависимост от избора:
   - въвежда число
   - извършва се операция върху дървото
4. Програмата работи, докато не бъде избран изход
